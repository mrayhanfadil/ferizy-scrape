# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.env
.DS_Store

# Temp
/tmp/
*.log

# Keep data but ignore local overrides
# (uncomment if you add secrets locally)
# .env.local

# Archives produced locally (source data lives in repo as files, zips are derived)
# ferizy-*.zip  # keep commented — if you prefer not to track zips, uncomment
